name: TEST

on:
  push:
  pull_request:
    types: [opened, synchronize, reopened]
  release:
    types: [published]

jobs:

  info:
    name: Info
    runs-on: windows-2022
    steps:

      - name: Check cat
        shell: bash
        run: |
          which cat
          ldd $(which cat)
          ls -la /c/strawberry/c/bin/*cat* || true
          ls -la /c/strawberry/c/bin/*cat* || true
          ls -la /c/mingw64/*cat* || true
