name: Build
on:
  push:
  pull_request:
    types: [opened, synchronize, reopened]
  release:
    types: [published]

jobs:
  build:
    name: Build
    runs-on: windows-11-arm
    steps:
      - name: ls
        shell: bash
        run: ls -la /
